# Test Environment Configuration
NODE_ENV=test

# Server Configuration
PORT=3001
HOST=localhost

# Database Configuration (Test Database)
MONGODB_URI=mongodb://localhost:27017/quiz-platform-test
MONGODB_DB_NAME=quiz-platform-test
MONGODB_OPTIONS=

# Redis Configuration (Test Redis)
REDIS_URL=redis://localhost:6379/1
REDIS_PASSWORD=
REDIS_DB=1

# JWT Configuration (Test Secrets)
JWT_SECRET=test-jwt-secret-key-that-is-long-enough-for-security
JWT_REFRESH_SECRET=test-jwt-refresh-secret-key-that-is-long-enough-for-security
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# OAuth Configuration (Test Credentials)
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback

# Email Configuration (Test SMTP)
SMTP_HOST=smtp.test.com
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASS=test-password
EMAIL_FROM=test@example.com
EMAIL_FROM_NAME=Quiz Platform Test

# Payment Gateway Configuration (Test Mode)
PAYPAL_CLIENT_ID=test-paypal-client-id
PAYPAL_CLIENT_SECRET=test-paypal-client-secret
PAYPAL_WEBHOOK_SECRET=test-paypal-webhook-secret

APS_API_KEY=test-aps-api-key
APS_MERCHANT_ID=test-aps-merchant-id
APS_WEBHOOK_SECRET=test-aps-webhook-secret

HYPERPAY_API_KEY=test-hyperpay-api-key
HYPERPAY_MERCHANT_ID=test-hyperpay-merchant-id
HYPERPAY_WEBHOOK_SECRET=test-hyperpay-webhook-secret

# Currency Exchange API (Test)
EXCHANGE_RATE_API_KEY=test-exchange-rate-api-key
FX_CACHE_TTL=300000

# Security Configuration (Test)
BCRYPT_ROUNDS=4
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000
CSRF_SECRET=test-csrf-secret-key-that-is-long-enough-for-security
SIGNED_URL_SECRET=test-signed-url-secret-key-that-is-long-enough-for-security

# CORS Configuration (Test)
CORS_ORIGIN=http://localhost:5173,http://localhost:3001
CORS_CREDENTIALS=true

# Monitoring & Logging (Test)
LOG_LEVEL=error
LOG_FORMAT=combined

# File Upload Configuration (Test)
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./test-uploads
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,application/pdf

# Cache Configuration (Test)
CACHE_TTL=300
CACHE_MAX_ITEMS=100

# Admin Configuration (Test)
ADMIN_EMAIL=admin@test.com
ADMIN_PASSWORD=TestAdmin123!

# Webhook Configuration (Test)
WEBHOOK_SECRET=test-webhook-secret-key-that-is-long-enough-for-security
WEBHOOK_TIMEOUT=5000

# Feature Flags (Test)
ENABLE_REGISTRATION=true
ENABLE_OAUTH=true
ENABLE_PAYMENTS=true
ENABLE_ANALYTICS=false
ENABLE_DEBUG_MODE=true

# API Configuration (Test)
API_BASE_URL=http://localhost:3001
CLIENT_URL=http://localhost:5173
SERVER_URL=http://localhost:3001

# Test Specific
MOCK_PAYMENTS=true
TEST_MODE=true
