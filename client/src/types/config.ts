/**
 * Configuration Types (Re-exports from config files with I prefix)
 * NOTE: Config files keep their original definitions for backwards compatibility
 */

// Re-export from config/constants.ts (these are kept in constants.ts for compatibility)
// Just document them here for reference
