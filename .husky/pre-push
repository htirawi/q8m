#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run core validation pipeline (skip unit tests due to legacy test setup issues)
pnpm typecheck && pnpm lint && pnpm build

# TODO: Re-enable unit tests after legacy test setup is fixed
# pnpm run test:unit